import React from "react";
import Default from "../layout/default.js";

export default function About() {
    return (
        <Default>
            <h1>History</h1>

            <p>We are building something from history.</p>
        </Default>
    );
}